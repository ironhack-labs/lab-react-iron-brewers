const ProjectCard = ({ title, description }) => {
    return (
        <div>
            <br></br>
            <p>{title}</p>
            <p>{description}</p>
        </div>
    )
}

export default ProjectCard